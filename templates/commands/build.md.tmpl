---
description: 프로젝트 빌드
---
프로젝트를 빌드합니다.

## 빌드 명령

(프로젝트 유형별 빌드 명령 — /init 시 자동 채움)

### 예시 (유형별)

- **iOS (SwiftUI)**: `xcodebuild -scheme <scheme> -sdk iphonesimulator build`
- **Android (Kotlin)**: `./gradlew assembleDebug`
- **Web (Next.js)**: `npm run build`
- **Flutter**: `flutter build`
